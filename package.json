{
  "name": "px-terravalue",
  "version": "1.0.0",
  "description": "TerraValue — parcel-level ecosystem services & land valuation API",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "node tests/e2e-validate.js",
    "test:e2e": "node tests/e2e-validate.js",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel"
  },
  "dependencies": {
    "@phloemxylem/terravalue-engine": "^1.0.2"
  }
}
